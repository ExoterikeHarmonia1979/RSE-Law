FIles/

# Real local config. .env.example is the tracked template - copy it to .env and
# fill in. Kept out of git because .env is where a key eventually gets added, even
# though today it holds only resource names and URLs.
.env

# graphify code-graph output - regenerated by `graphify . --code-only`.
graphify-out/
