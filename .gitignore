# Isolated workspaces for subagent-driven runs. Scratch, never source.
.claude/worktrees/

# Subagent-driven-development ledger and review packages.
.superpowers/

# Redemption is a commercial COM library, licensed per-developer and not ours to
# redistribute. Keep the archive and any extraction out of the repo.
Redemption.zip
Redemption*.dll
Interop.Redemption.dll
.local/

# graphify writes graphify-out/ wherever it is invoked. Derived from tracked source.
graphify-out/

__pycache__/
*.pyc

# Peeked Service Bus messages - live Graph resource ids for real mail, not source.
infra/logicapps/tools/dlq-preserved/

# Ingest working directory: unpacked export packages, up to ~204 GB.
ingest-work/
rse-ingest-work/

# reindex-touch.ps1 state: hundreds of thousands of lines, rebuildable.
infra/logicapps/tools/reindex-touched.txt
